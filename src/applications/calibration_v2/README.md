# Deprecated

`src.applications.calibration_v2` is deprecated and is not wired into any active
robot system.

Use `src.applications.calibration` for calibration application work. Keep this
package only as historical/reference code unless a future task explicitly
reactivates it.
